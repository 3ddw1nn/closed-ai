---
interface Props {
  size?: number;
  class?: string;
}

const { size = 48, class: className = "" } = Astro.props;
---

<img
  class={`wacky-logo ${className}`}
  src="/closedai-logo.svg"
  width={size}
  height={size}
  alt=""
  aria-hidden="true"
  loading="eager"
  decoding="async"
/>
